name: mini_pos_checkout_core
description: "A Mini-POS Checkout Core — BLoC, pure Dart, fully unit–tested"

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.1.3
  equatable: ^2.0.5
  hydrated_bloc: ^9.1.2
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.1.5
  flutter_lints: ^4.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/catalog.json
